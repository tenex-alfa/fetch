template:
  url: String
  out?: String
  body?: {}
  method?: -get
    -post
    -put
    -delete

mock:
  name: myTestResource
  args:
    - url: https://api.github.com/orgs/tenex-alfa
      out: public_repos
