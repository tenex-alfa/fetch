"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const parse_yml_1 = require("./parse-yml");
const validate_1 = require("./validate");
const validate = (input, template, config) => {
    const parsedInput = parse_yml_1.default(input);
    const parsedTemplate = parse_yml_1.default(template);
    const valid = validate_1.default(parsedInput, parsedTemplate);
    if (!valid && config && config.noExeceptions)
        return false;
    else {
        if (!valid)
            throw new Error(validate_1.ErrorMessage());
        else
            return parsedInput;
    }
};
exports.validate = validate;
exports.default = validate;
